"""pins — STM32 target pin/switch tooling (folded in from STMP).

Canonical switch-cell engine (``switch_engine``) + report renderer
(``switch_report``). A socket pin needs a switch when it takes >=2 distinct
routing identities across the package family.
"""
